server:
  port: 8080
spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          #配置Git仓库的地址
          uri: https://gitee.com/zpyambition/spring-cloud-config-repo
          #Git仓库的账号
          username: 18003711149
          #Git仓库的密码
          password: my_930810

